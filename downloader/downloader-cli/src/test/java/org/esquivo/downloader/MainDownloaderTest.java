package org.esquivo.downloader;


/**
 * The Class URLDownloaderTest.
 */
public class MainDownloaderTest {
	
	/*
	@Test
	public void goodParamsWillDownloadFileURL() throws IOException {
		// GIVEN : A good parameter list
		String args[] = { "-u", "http://google.com" };
		
		
		// WHEN : Download from valid URL
		Main.main(args);
		
		// THEN : The file is stored
		//assertTrue(file.exists());
	}
	

	@Test
	public void () throws IOException {
		// GIVEN : A download class
		
		// WHEN : Download form invalid URL
		down.download(new URL("http://mierd"));
		
		// THEN : Fails
		fail("Exception not throw");
	}
	*/
	
	/*
	@Test
	public void unreconizedOptionCausesNullException() throws IOException {
		// GIVEN : A good parameter list
		String args[] = { "-p", "http://google.com" };

		// WHEN : Download form valid URL
		Main.main(args);
		
		// THEN : The file is stored
		//assertTrue(file.exists());
	}
	*/
}
